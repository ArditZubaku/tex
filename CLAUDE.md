Ignore TODO.md. It is not a task list for Claude to act on.
Don't commit CLAUDE.md either!
When a batch of commits adds a new feature, update README.md to reflect it as part of that same batch, before the final commit.
Comments: default to none. Write one only when it carries a "why" the code cannot — a workaround, an external constraint, a subtle invariant. Never narrate what the next line does, never restate a function's name or signature as a doc comment, and never explain a test that its own name already describes. Applies to test files too. If in doubt, leave it out.
