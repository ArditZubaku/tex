- [ ] make the code simpler, more readable, but basically simpler so one doesn't have to grasp too much to understand what is happening, use AI to do that
- [ ] pprof it and try to squeeze every bit of performance possible
  - [ ] try pprofing yourself first, but then make AI do it
- [ ] replace everything with tcell or bubbletea (whichever is more popular)
- [ ] rewrite this in Rust!!!!!!!!!!!!
- [ ] Analyze how some of the VIM features were implemented like the
      G, A, I, w, e
